---
en:
  activerecord:
    errors:
      models:
        address:
          attributes:
            postcode:
              invalid: is not in the right format
        applicant:
          attributes:
            date_of_birth:
              invalid: is not valid
            email:
              invalid: address is not in the right format
            national_insurance_number:
              invalid: is not in the right format
        announcement:
          attributes:
            display_type:
              blank: Select a display type
            heading:
              blank: Include, at minimum, a heading
            body:
              blank: Include, at minimum, some body text
            link_url:
              blank: Include a link URL if setting display text
            link_display:
              blank: Include link display text if setting a link URL
            start_at:
              blank: Set a start date/time
              unparseable: Enter a valid start date and time
            end_at:
              blank: Set an end date/time
              unparseable: Enter a valid end date and time
        application_proceeding_type:
          attributes:
            lead_proceeding:
              multiple: Only one application proceeding type can be nominated as lead proceeding
        feedback:
          attributes:
            satisfaction:
              blank: Select how satisfied you were with the service
            difficulty:
              blank: Select how easy or difficult it was to apply for civil legal aid using the new service
            done_all_needed:
              inclusion: Select yes if you were you able to do what you needed today
            contact_name:
              blank: Enter a contact name and email or neither
            contact_email:
              blank: Enter a contact name and email or neither
              invalid: Enter a valid email address
        legal_aid_application:
          attributes:
            base:
              none_selected: Select if you receive any types of income
        legal_framework/merits_task_list:
          attributes:
            task_list:
              incomplete: You must complete every section before you can continue
        linked_application:
          attributes:
            lead_application_id:
              cannot_link_self: Application cannot be linked to itself
        hmrc/response:
          attributes:
            use_case:
              blank: can't be blank
              inclusion: Invalid use case
