name: Static Application Security Testing
on:
  schedule:
    - cron:  '15 6 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  run-sast-tooling:
    uses: ministryofjustice/laa-reusable-github-actions/.github/workflows/sast.yml@da8dfead60c43877af5a5e72ffa8c72301cac18f # main as of 12/06/2026
    permissions:
      id-token: write
      contents: read
      security-events: write
      actions: read
    with:
      image_uri: ${{ format('{0}:latest', vars.ECR_REGISTRY_URI) }}
    secrets:
      SNYK_CLIENT_ID: ${{ secrets.SNYK_CLIENT_ID }}
      SNYK_CLIENT_SECRET: ${{ secrets.SNYK_CLIENT_SECRET }}
      ECR_ROLE_TO_ASSUME: ${{ secrets.ECR_ROLE_TO_ASSUME }}
