apiVersion: v1
kind: Service
metadata:
  name: {{ template "apply-for-legalaid.fullname" . }}
  labels:
    app: {{ template "apply-for-legalaid.name" . }}
    chart: {{ template "apply-for-legalaid.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apply-for-legalaid.name" . }}
    release: {{ .Release.Name }}
