<%= date_picker_field(
      form,
      field_name,
      # TODO: 6303/6306 handle displying of database value for the model -  e.g. value: params.dig("proceeding", "hearing_date") || <from DB>
      label: { text: t("shared.forms.date_input_fields.scope_limitations_label"), class: "govuk-label govuk-date-input__label" },
      hint: { text: t("shared.forms.date_input_fields.scope_limitations_hint", options: number_of_days_ago(0, Date::DATE_FORMATS[:date_picker])) },
    ) %>
