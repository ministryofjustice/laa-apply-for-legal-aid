    <%= form_with(
          model: @form,
          builder: GOVUKDesignSystemFormBuilder::FormBuilder,
          url: form_path,
          method: :patch,
          local: true
      ) do |form| %>

    <%= form.govuk_radio_buttons_fieldset :provider_received_citizen_consent,
                                          legend: {size: 'xl', tag: 'h1', text: page_title} do %>

        <p class="govuk-body govuk-!-padding-top-4">
          <%= info %>
        </p>

        <ul class="govuk-list govuk-list--bullet">
          <li><%= t('providers.open_banking_consents.show.list_1') %></li>
          <li><%= t('providers.open_banking_consents.show.list_2_html') %></li>
          <li><%= t('providers.open_banking_consents.show.list_3') %></li>
        </ul>

        <%= form.govuk_radio_button(
                :provider_received_citizen_consent,
                true,
                label: {text: t('providers.open_banking_consents.show.radio_yes')}
            ) %>

        <%= form.govuk_radio_button(
                :provider_received_citizen_consent,
                false,
                label: {text: t('providers.open_banking_consents.show.radio_no')}
            ) %>

        <div class="govuk-!-padding-bottom-5"></div>

        <%= next_action_buttons(
            show_draft: local_assigns.key?(:show_draft) ? show_draft : false,
            form: form
        ) %>
    <% end %>
  <% end %>
