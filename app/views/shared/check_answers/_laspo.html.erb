<section>
  <%= govuk_summary_card(title: t(".heading")) do |card| %>
    <% unless read_only %>
      <% card.with_action do %>
        <%= govuk_link_to(t("generic.change"),
                          providers_legal_aid_application_in_scope_of_laspo_path(@legal_aid_application)) %>
      <% end %>
    <% end %>
    <%= card.with_summary_list(actions: !read_only) do |summary_list| %>
      <%= summary_list.with_row(html_attributes: { id: "app-check-your-answers__in_scope_of_laspo" }) do |row| %>
        <%= row.with_key(text: t(".question"), classes: "govuk-!-width-one-half") %>
        <%= row.with_value(text: yes_no(@legal_aid_application.in_scope_of_laspo)) %>
      <% end %>
    <% end %>
  <% end %>
</section>
