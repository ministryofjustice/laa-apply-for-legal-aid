<% if current_provider %>
  <%= render(partial: "shared/partials/cookie_banner") if display_cookie_banner?(current_provider) %>
<% end %>

<%= govuk_header(service_name: t("layouts.application.header.title"), service_url: home_path) { |header| user_header_navigation(header) } %>
