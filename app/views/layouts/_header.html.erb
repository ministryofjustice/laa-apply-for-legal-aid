<% if current_provider %>
  <%= render(partial: "shared/partials/cookie_banner") if display_cookie_banner?(current_provider) %>
<% end %>

<%= render MojComponent::HeaderComponent.new(organisation_name: t(".organisation_name"), url: "#", nav_items: user_header_navigation) %>
