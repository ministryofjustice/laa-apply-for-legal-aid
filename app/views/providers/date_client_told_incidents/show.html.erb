<%= page_template page_title: t('.h1-heading'), template: :form do %>

  <%= form_with(
        model: @form,
        url: providers_legal_aid_application_date_client_told_incident_path,
        method: :patch,
        local: true
      ) do |form| %>

    <%= date_input_fields prefix: :told, field_name: :told_on, width: :full, form: form %>

    <%= next_action_buttons(show_draft: true, form: form) %>

  <% end %>

<% end %>
