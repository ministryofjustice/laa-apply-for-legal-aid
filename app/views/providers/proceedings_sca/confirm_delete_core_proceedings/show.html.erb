<h1 class="govuk-heading-xl"><%= "Are you sure you want to delete '#{@proceeding.meaning}'" %></h1>

<p>These proceedings will also be deleted:</p>

<ul class="govuk-list govuk-list--bullet">
  <% @legal_aid_application.related_proceedings.each do |proceeding| %>
    <li><%= proceeding.meaning %></li>
  <% end %>
</ul>

<%= govuk_warning_text(text: "You cannot undo this action") %>

<div class='govuk-button-group'>
  <%= form_for(@legal_aid_application, url: providers_legal_aid_applications_path) do |form| %>
    <%= form.govuk_submit(
          "Yes, delete it",
          warning: true,
        ) %>
    <%= govuk_button_link_to "No, do not delete it", providers_legal_aid_applications_path %>
  <% end %>
</div>
