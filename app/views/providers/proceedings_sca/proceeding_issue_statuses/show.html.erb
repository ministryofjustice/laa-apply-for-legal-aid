<%= form_with(
      url: providers_legal_aid_application_proceeding_issue_statuses_path,
      model: @form,
      method: :patch,
      local: true,
    ) do |form| %>
  <%= page_template page_title: t(".page_title"),
                    template: :basic,
                    form: do %>
    <%= form.govuk_collection_radio_buttons :proceeding_issue_status,
                                            yes_no_options,
                                            :value,
                                            :label,
                                            legend: { text: t(".page_title"), size: "xl", tag: "h1" },
                                            caption: { text: @proceeding.meaning, size: "xl" } do %>

    <% end %>

    <%= next_action_buttons(show_draft: true, form:) %>
  <% end %>
<% end %>
