<%= form_with(
      model: @form,
      url: providers_legal_aid_application_open_banking_guidance_path(@legal_aid_application),
      method: :patch,
      local: true
    ) do |form| %>

<% end %>
