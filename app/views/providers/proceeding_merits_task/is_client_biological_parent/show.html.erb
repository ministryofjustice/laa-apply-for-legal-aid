<%= form_with(model: @form,
              url: providers_merits_task_list_is_client_biological_parent_path(@proceeding),
              method: :patch,
              local: true) do |form| %>
  <%= page_template page_title: t(".page_title"), template: :basic, form: do %>
    <% options = [
         [:biological, t("generic.yes")],
         [false, t("generic.no")],
       ] %>

    <%= form.govuk_collection_radio_buttons :relationship_to_child,
                                            options,
                                            :first,
                                            :second,
                                            hint: { text: "" },
                                            legend: { text: page_title, tag: "h1", size: "l" },
                                            caption: { text: @proceeding.meaning, size: "xl" } %>

    <%= next_action_buttons show_draft: true, form: %>
  <% end %>
<% end %>
