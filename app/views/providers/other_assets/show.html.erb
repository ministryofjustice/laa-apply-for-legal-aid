<% content_for :navigation do %>
  <%= link_to t('generic.back'), providers_root_path, class: 'govuk-back-link', id: 'back' %>
<% end %>

<% content_for(:page_title) { t('.h1-heading') } %>

<%= render partial: 'shared/forms/error_summary', locals: { model: @form } %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl"><%= content_for(:page_title) %></h1>
  </div>
</div>

<%= render partial: '/citizens/other_assets/form', locals: { model: @form, url: providers_legal_aid_application_other_assets_path(@legal_aid_application) } %>
