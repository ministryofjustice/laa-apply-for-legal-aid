<div class="govuk-accordion__section-content">
  <h2 class="govuk-heading-m"><%= t('.title') %></h2>
  <table class="govuk-table">
    <tbody>
    <%= render partial: 'results_detail_row', locals: { caption: t('.total_income'), value: number_to_currency(@cfe_result.monthly_state_benefits) } %>
    <%= render partial: 'results_detail_row', locals: { caption: t('.total_outgoings'), value: number_to_currency(@cfe_result.monthly_state_benefits) } %>
    <%= render partial: 'results_detail_row', locals: { caption: t('.total_deductions'), value: number_to_currency(@cfe_result) } %>
    <%= render partial: 'results_total_row', locals: { caption: t('.total_disposable'), value: number_to_currency(@cfe_result.total_gross_income) } %>
    </tbody>
  </table>
</div>
