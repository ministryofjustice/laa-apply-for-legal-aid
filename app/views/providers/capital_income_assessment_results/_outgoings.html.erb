<div class="govuk-accordion__section-content">
  <h2 class="govuk-heading-m"><%= t('.title') %></h2>
  <table class="govuk-table">
    <tbody>
    <%= render partial: 'results_detail_row', locals: { caption: t('.housing_costs'), value: number_to_currency(@cfe_result.monthly_state_benefits) } %>
    <%= render partial: 'results_detail_row', locals: { caption: t('.childcare_costs'), value: number_to_currency(@cfe_result) } %>
    <%= render partial: 'results_detail_row', locals: { caption: t('.maintenance_out'), value: number_to_currency(@cfe_result.maintenance_per_month) } %>
    <%= render partial: 'results_detail_row', locals: { caption: t('.legal_aid'), value: number_to_currency(@cfe_result) } %>
    <%= render partial: 'results_total_row', locals: { caption: t('.total_outgoings'), value: number_to_currency(@cfe_result.total_gross_income) } %>
    </tbody>
  </table>
</div>
