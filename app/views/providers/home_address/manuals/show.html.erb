<%= form_with(
      model: @form,
      scope: :address,
      url: providers_legal_aid_application_home_address_manual_path(@legal_aid_application),
      method: :patch,
      local: true,
    ) do |form| %>

  <%= render "shared/addresses", location: "home", form: %>
<% end %>
