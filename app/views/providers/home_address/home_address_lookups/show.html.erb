<%= form_with(
      model: @form,
      scope: :address_lookup,
      url: providers_legal_aid_application_home_address_home_address_lookup_path(@legal_aid_application),
      method: :patch,
      local: true,
    ) do |form| %>

  <%= render "shared/address_lookup", location: "home", form: %>
<% end %>
